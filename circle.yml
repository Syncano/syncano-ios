machine:
  xcode:
    version: "6.4"
  environment:
    XCODE_WORKSPACE: syncano-ios.xcworkspace
    XCODE_SCHEME: "Syncano"
dependencies:
  pre:
    - brew uninstall xctool && brew install --HEAD xctool